这里的核心不是“直接看出范数凸”，而是把矩阵范数改写成一族关于矩阵 $X$ 的线性函数的逐点上确界。

先看谱范数:

$$
f(X) = \\|X\\|_2 = \\sup \\{ u^T X v \\mid \\|u\\|_2 = 1, \\|v\\|_2 = 1 \\}.
$$

为什么这个式子对? 对固定的 $v$, 有

$$
\\sup_{\\|u\\|_2 = 1} u^T Xv = \\|Xv\\|_2.
$$

于是再对所有满足 $\\|v\\|_2 = 1$ 的 $v$ 取上确界:

$$
\\sup_{\\|v\\|_2 = 1} \\|Xv\\|_2 = \\|X\\|_2.
$$

关键点在于: 当 $u, v$ 固定时,

$$
X \\mapsto u^T Xv
$$

是关于 $X$ 的线性函数。所以 $\\|X\\|_2$ 是一族线性函数的上确界。线性函数是凸函数，而任意一族凸函数的逐点上确界仍然是凸函数，因此谱范数是凸函数。

下面是一般诱导范数的同一套逻辑。设 $\\|\\cdot\\|_a$ 是 $\\mathbb{R}^p$ 上的范数, $\\|\\cdot\\|_b$ 是 $\\mathbb{R}^q$ 上的范数, 矩阵 $X \\in \\mathbb{R}^{p \\times q}$. 诱导范数定义为

$$
\\|X\\|_{a,b}
=
\\sup_{v \\neq 0} \\frac{\\|Xv\\|_a}{\\|v\\|_b}.
$$

因为比值对 $v$ 是齐次的，可以把 $v$ 归一化为 $\\|v\\|_b = 1$:

$$
\\|X\\|_{a,b}
=
\\sup \\{ \\|Xv\\|_a \\mid \\|v\\|_b = 1 \\}.
$$

再用对偶范数公式:

$$
\\|z\\|_a
=
\\sup \\{ u^T z \\mid \\|u\\|_{a*} = 1 \\}.
$$

令 $z = Xv$, 就得到

$$
\\|X\\|_{a,b}
=
\\sup \\{ u^T Xv \\mid \\|u\\|_{a*} = 1, \\|v\\|_b = 1 \\}.
$$

所以一般诱导范数也被写成了一族关于 $X$ 的线性函数 $u^T Xv$ 的上确界，因此它是凸函数。

一句话总结:

$$
\\text{矩阵范数}
=
\\sup \\{ \\text{关于 } X \\text{ 的线性函数} \\}
\\quad \\Longrightarrow \\quad
\\text{矩阵范数是凸函数}.
$$
