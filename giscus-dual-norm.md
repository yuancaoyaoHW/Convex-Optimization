对偶范数可以理解成: 用内积去测试一个向量时, 在原范数的单位球里能测出的最大值。

如果原范数是 $\\|\\cdot\\|$, 它的对偶范数定义为

$$
\\|u\\|_* = \\sup_{\\|x\\| \\le 1} u^T x.
$$

意思是: 在所有满足 $\\|x\\| \\le 1$ 的向量里, 找一个最能和 $u$ 对齐的 $x$, 看内积 $u^T x$ 最大能到多少。

它最重要的作用是给出广义 Cauchy-Schwarz 不等式:

$$
u^T x \\le \\|u\\|_* \\|x\\|.
$$

几个常见例子不要放进 Markdown 表格里, 因为范数符号里的竖线容易和表格分隔符冲突。写成分行公式更稳:

$$
\\|\\cdot\\|_2 \\quad \\text{的对偶范数还是} \\quad \\|\\cdot\\|_2.
$$

$$
\\|\\cdot\\|_1 \\quad \\text{的对偶范数是} \\quad \\|\\cdot\\|_\\infty.
$$

$$
\\|\\cdot\\|_\\infty \\quad \\text{的对偶范数是} \\quad \\|\\cdot\\|_1.
$$

$$
\\|\\cdot\\|_p \\quad \\text{的对偶范数是} \\quad \\|\\cdot\\|_q,
\\quad
\\frac{1}{p} + \\frac{1}{q} = 1.
$$

截图里用的是这个公式:

$$
\\|z\\|_a
=
\\sup \\{ u^T z \\mid \\|u\\|_{a*} = 1 \\}.
$$

这个公式的目的, 是把原来非线性的范数 $\\|Xv\\|_a$ 改写成一个上确界:

$$
\\|Xv\\|_a
=
\\sup \\{ u^T Xv \\mid \\|u\\|_{a*} = 1 \\}.
$$

这样固定 $u, v$ 后, $u^T Xv$ 就是关于矩阵 $X$ 的线性函数。于是诱导范数可以写成

$$
\\|X\\|_{a,b}
=
\\sup \\{ u^T Xv \\mid \\|u\\|_{a*} = 1, \\|v\\|_b = 1 \\}.
$$

所以诱导范数是一族关于 $X$ 的线性函数的逐点上确界, 因此它是凸函数。
